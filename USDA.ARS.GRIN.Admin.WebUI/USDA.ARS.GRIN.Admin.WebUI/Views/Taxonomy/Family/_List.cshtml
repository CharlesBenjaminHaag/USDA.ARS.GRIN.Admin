@model USDA.ARS.GRIN.Admin.WebUI.ViewModels.Taxonomy.FamilyListViewModel
<div class="mailbox-controls">
    @*<button type="button" class="btn btn-success btn-sm check-all-search-results">
            <i class="fa fa-check"></i>
        </button>
        <button type="button" class="btn btn-danger btn-sm uncheck-all-search-results">
            <i class="fa fa-check-square"></i>
        </button>*@
    @*<button type="button" class="btn btn-primary btn-sm" onclick="location.href='@Url.Action("CropTraitEdit", "Taxonomy", new { speciesId = Model.SpeciesID, cropId = Model.CropId, cropMapId = Model.CropMapID, cropTraitid = 0 })'">
            <i class="fa fa-plus"></i>
        </button>*@
    @*<button type="button" id="btnDeleteMultiple" class="btn btn-danger btn-sm">
            <i class="fa fa-trash"></i>
        </button>*@

    <button type="button" id="btnAddItemsToFolder" class="btn btn-primary btn-sm" data-toggle="modal" data-target="#modal-add-items-to-folder">
        <i class="fa fa-folder"></i>
    </button>
    <button type="button" class="btn btn-primary btn-sm" onclick="location.href='@Url.Action("FamilyEdit", "Taxonomy", new { id = 0 })'">
        <i class="fa fa-plus"></i>
    </button>
    <div class="pull-right">
        <button type="button" disabled class="btn btn-default btn-sm">
            Export
        </button>
    </div>
</div>
<table id="data-table" class="table table-striped" style=" width:100%;">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Authority</th>
            <th>Last Modified</th>
            <th>By</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var family in Model.Families)
        { 
        <tr>
            <td>
                <span class="badge">@family.ID</span>
            </td>
            <td>
                @family.Name
            </td>
            <td>
                @family.Authority
            </td>
            <td>
                @family.ModifiedDate.ToShortDateString()
            </td>
            <td>
                @family.ModifiedByCooperatorName
            </td>
            <td>
                <a href="@Url.Action("FamilyEdit", "Taxonomy", new { id = family.ID })" class="btn btn-primary btn-sm pull-right row-edit">
                    <i class="fa fa-edit"></i>
                </a>
            </td>
        </tr>
        }
    </tbody>
</table>

<script type="text/javascript">
    $('#data-table').DataTable(
        {
            responsive: true,
            select: true
        }
    );
</script>